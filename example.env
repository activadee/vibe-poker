# Rooms backend
# memory (default) keeps rooms in-process. Use redis in production/HA.
ROOMS_BACKEND=redis

# Redis connection URL
# Used when ROOMS_BACKEND=redis and for Socket.IO Redis adapter
REDIS_URL=redis://localhost:6379

# Redis ACL username/password (optional)
# If provided, they are passed to the Redis client in addition to REDIS_URL
REDIS_USERNAME=
REDIS_PASSWORD=

# Optional: comma-separated list of allowed CORS origins for Socket.IO and HTTP
# Example: https://app.example.com,https://staging.example.com
CORS_ALLOWLIST=
